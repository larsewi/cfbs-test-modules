bundle agent create_multiple_files
# @brief Create multiple files.
{
  meta:
      "tags"
        slist => { "autorun" };

  files:
      "$(cfbs:create_multiple_files.input)"
        create => "true";
}
